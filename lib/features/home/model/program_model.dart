class ProgramModel{
  final String title, totalModules, totalDuration, imagePath;
  ProgramModel({
    required this.title,
    required this.totalModules,
    required this.totalDuration,
    required this.imagePath,
  });
}