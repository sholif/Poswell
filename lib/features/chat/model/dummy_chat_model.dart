class DummyChatModel{
   final String title, lastMessage, time, imageUrl;
  final int unreadCount;
  const DummyChatModel({
    required this.title,
    required this.lastMessage,
    required this.time,
    required this.imageUrl,
    required this.unreadCount,
  });
}